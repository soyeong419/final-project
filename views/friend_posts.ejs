<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= friendId %>님의 게시물</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .id {
      text-align: center;
      margin: 10px 330px 30px auto;
      font-size: 27px;
    }
    .post-card {
      background: #f9f9f934;
      border: 1px solid #ddd;
      border-radius: 12px;
      padding: 25px 30px;
      margin: 0 auto 40px auto; 
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08); 
      height: auto;
      width: 50%;
    }

    .post-title {
      font-size: 25px;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .post-content {
      font-size: 1.1em;
      margin-bottom: 12px;
    }

    .post-date {
      font-size: 0.9em;
      color: #888;
    }
  </style>
</head>
<body>
  <%- include('menu.ejs') %> <!-- 별도의 EJS 파일로 분리한 menu를 불러옴 -->
  <div class="container mt-4">
    <h2 class="id"><%= friendId %>님의 게시물</h2> <!-- 친구의 id를 제목으로 출력 -->

    <% if (posts.length === 0) { %> <!-- 게시물이 없으면 출력 -->
      <p>게시물이 없습니다.</p>
    <% } else { %>
      <% posts.forEach(post => { %> <!-- 게시물이 있으면 반복하여 출력 -->
        <div class="post-card">
          <div class="post-title"><%= post.title %></div>
          <div class="post-content"><%= post.content %></div>
          <% if (post.path) { %> <!-- 게시물 이미지가 있는 경우 출력 -->
            <img src="<%= post.path %>" alt="게시물 사진" 
            style="max-width: 100%; margin-bottom: 15px; border-radius: 8px;">
          <% } %>
          <div class="post-date"><%= post.date.toLocaleString() %></div>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
