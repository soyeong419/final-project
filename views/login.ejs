
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .login {
      padding: 70px 60px;
      width: 360px;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.178);
      border-radius: 10px;
    }
    .login label {
      display: inline-block;
      width: 80px;
      margin-right: 15px;
      text-align: right;
    }
    .login input {
      width: 220px;
      padding: 8px 12px;
      margin-bottom: 20px;
      text-align: left; 
    }
    .login button {
        width: 100%; 
        padding: 15px 40px; 
        border: none; 
        margin: 0 auto;
        display: block; 
        border-radius: 10px;
        background-color: rgba(46, 157, 255, 0.89); 
        color: white; 
    }
    .container{
      margin-top: 8px;
      padding: 0px 15px;
      text-align: right;
    }
    .signup {
      display: inline-block;
      color: #9c9c9c;
      text-decoration: none;
      font-size: 15px;
    }
    .signup:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <% if (typeof error !== 'undefined') { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>
  <form class="login" action="/login" method="post">
    <div class="form-group">
      <label>아이디</label>
      <!-- 사용자 아이디 입력 필드 -->
      <input type="text" name="userid" class="form-control">
    </div><p></p>
    <div class="form-group">
      <label>비밀번호</label>
      <!-- 비밀번호 입력 필드 -->
      <input type="password" name="userpwd" class="form-control">
    </div><p></p>
    <button type="submit">로그인</button> <!-- 폼 전송 버튼 -->
    <div class="container">
      <!-- 회원가입 페이지로 이동하는 링크 -->
      <a href="/signup" class="signup">회원가입</a>
    </div>
  </form>
</body>
</html>
