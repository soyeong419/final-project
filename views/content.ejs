<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
  <style>
    body {
    background-color: #ffffff; 
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    }
    main.container {
    display: flex;
    justify-content: center;  
    padding: 32px 16px; 
    margin-top: 32px;
    min-height: 10px;
    }
    .card {
    max-width: 500px;
    width: 100%;
    box-shadow: 0 4px 15px rgb(0 0 0 / 0.1);
    border-radius: 12.8px;
    background: #fff;
    }
    .card img {
    border-radius: 8px; 
    margin-top: 16px; 
    display: block;
    max-width: 100%;
    height: auto;
    }
    .card-subtitle {
    color: #6c757d;
    font-size: 14.4px;  
    margin-top: 12.8px; 
    }
    .card-link {
    margin-right: 16px;
    }
  </style>
</head>
<body>
  <%- include('menu.ejs') %> <!-- 별도의 EJS 파일로 분리한 menu를 불러옴 -->
  <main class="container"> <!-- 메인 컨텐츠 영역을 담는 컨테이너 -->
    <div class="card">
      <div class="card-header">
        <!-- 게시글 제목 출력. data 객체의 title 속성 사용 -->
        <h4 class="card-title mt-3 mb-3"><%= data.title %></h4>
      </div>
      <div class="card-body">
        <!-- 게시글 내용 출력. data 객체의 content 속성 사용 -->
        <p class="card-text"><%= data.content %></p>
        <!-- 게시글에 첨부된 이미지가 있을 경우 이미지 태그로 출력 -->
        <% if(data.path) { %>
          <!-- data.path에 저장된 이미지 경로를 src로 설정, 이미지가 없으면 alt 속성 -->
          <img src="<%= data.path %>" alt="첨부 이미지" />
        <% } %>
        <!-- 작성일과 작성자 정보 -->
        <p class="card-subtitle mb-2">작성일 : <%= data.date %> / 작성자 : <%= data.userid %></p>
        <!-- 게시글 수정 페이지로 이동하는 링크 -->
        <a href="/edit/<%= data._id %>" class="card-link btn btn-outline-primary btn-sm">수정하기</a>
        <!-- 게시글 목록 페이지로 돌아가는 링크 -->
        <a href="/list" class="card-link btn btn-outline-secondary btn-sm">목록</a>
      </div>
    </div>
  </main>
</body>
</html>
