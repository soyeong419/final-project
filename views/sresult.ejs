<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
</head>
    <body>
    <%- include('menu.ejs') %> <!-- 별도의 EJS 파일로 분리한 menu를 불러옴 -->
        <h2 class="ml-2 my-3 text-center">검색 결과</h2>
        <div class="container">
            <ul class="list-group">
                <% for(var i=0; i<data.length; i++){ %> <!--  data 배열을 순회하여 각 항목을 리스트로 표시 -->
                    <li class="list-group-item">
                        <div class="card" style="width: 100%;">
                            <div class="card-header">
                                <h4 class="card-title"><%= data[i].title %></h4>
                            </div>
                            <div class="card-body">
                                <!-- 게시글 본문 내용 출력 -->
                                <p class="card-text"><%= data[i].content %></p>     
                                <!-- 이미지가 있다면 출력 -->
                                <img src="<%= data[i].path %>" width="200"><p></p>
                                <p class="card-subtitle mb-2">작성일 : <%= data[i].date %></p>
                                <!-- 수정 페이지로 이동하는 링크 -->
                                <a href="/edit/<%= data[i]._id %>" class="card-link">수정하기</a>
                                <!-- 목록 페이지로 이동하는 링크 -->
                                <a href="/list" class="card-link">목록</a>
                            </div>
                        </div>
                    </li>
                <% } %>
            </ul>
        </div>
    </body>
</html>